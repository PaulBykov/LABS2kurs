CREATE VIEW Лекции(Идентификатор, Группа, Преподаватель, Предмет)
	AS SELECT Идентификатор, Номер_группы, Код_преподавателя, Предмет
	FROM Курсы
	WHERE Тип_занятия = 'ЛК'
	go
	SELECT * FROM Лекции

INSERT Лекции values(10, 2, 3, 'БД')
INSERT Лекции values(11, 4, 2, 'БД')
